from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Building a model that will predict if a college basketball player will be drafted to join the NBA league.',
    author='Monali',
    license='MIT',
)
